import React from 'react';

// Styles
import './SideText.styles.scss';

const SideText = ({ content }) => (
    <div className='side-text'>
        <p>{content}</p>
    </div>
)

export default SideText;